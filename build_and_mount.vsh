#!/usr/bin/env -S v -raw-vsh-tmp-prefix tmp

// #!/usr/bin/env -S v -cc tcc -showcc -show-timings -raw-vsh-tmp-prefix tmp

// TODO: qemu-img create -f raw disk_image.raw 30G


fn main() {
  println('hello world')
}